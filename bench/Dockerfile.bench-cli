# Variant of the bench image with the Docker CLI installed, used to
# measure Sentinel's docker-CLI subprocess code path with the socket present.
FROM sentinel-bench
RUN apt-get update \
 && apt-get install -y --no-install-recommends docker.io \
 && rm -rf /var/lib/apt/lists/*
